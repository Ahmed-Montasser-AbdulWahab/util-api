apiVersion: v1
kind: Service
metadata:
  name: util-api-service
  labels:
    app: util-api-service
spec:
  # Service type (ClusterIP, NodePort, LoadBalancer, ExternalName)
  type: ClusterIP
  
  # Port mappings
  ports:
    - name: http
      port: 7000
      targetPort: 7000
      protocol: TCP
  
  # Selector to match pods
  selector:
    app: util-api
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: util-api-dep
  name: util-api-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: util-api
  strategy: 
    type: Recreate
  template:
    metadata:
      labels:
        app: util-api
    spec:
      containers:
      - image: ahmedmontasser/util-api:v1
        # imagePullPolicy: Always
        name: util-api
        ports:
          - containerPort: 7000